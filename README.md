# 🍺 SkufUp - Детектор Пива

**SkufUp** — программа, которая слушает микрофон и ждёт звук открытия банки пива.  
Когда слышит характерный "пшик" — запускает игру или открывает сайт.

---

## 📁 Структура проекта

```
Открытый код/
├── gui_app.py              # Главное приложение с интерфейсом
├── audio_detector.py       # Детектор звука (сравнение с эталоном)
├── beer_sound_template.py  # Эталонные звуки открытия банки
├── config.py               # Настройки (пороги, cooldown и т.д.)
├── build_installer.py      # Скрипт для сборки инсталлятора
├── create_icon.py          # Генератор иконок
├── requirements.txt        # Зависимости Python
├── beer.ico                # Иконка программы
└── setup.ico               # Иконка инсталлятора
```

---

## 🚀 Быстрый старт

### 1. Установи зависимости
```bash
pip install -r requirements.txt
```

### 2. Запусти программу
```bash
python gui_app.py
```

### 3. Выбери игру или сайт

### 4. Нажми СТАРТ и открой пиво! 🍺

---

## 🔧 Как работает детекция

### Алгоритм распознавания

1. **Запись звука** — микрофон постоянно записывает аудио
2. **Фильтрация по громкости** — тихие звуки игнорируются
3. **Извлечение характеристик** — для громких звуков вычисляем:
   - Огибающую (как меняется громкость во времени)
   - Спектр (какие частоты присутствуют)
   - Позицию пика (где самый громкий момент)
   - Скорость нарастания (attack)
   - Скорость затухания (decay)
4. **Сравнение с эталоном** — сравниваем с записью открытия банки
5. **Принятие решения** — если похожесть > 55%, это пиво!

### Почему два эталона?

- `template_close` — запись с близкого расстояния (чёткий звук)
- `template_far` — запись с дальнего расстояния (тише, больше эха)

Программа сравнивает с обоими и берёт лучший результат.

---

## 📦 Сборка .exe

### Основная программа
```bash
pyinstaller --onefile --windowed --name=SkufUp --icon=beer.ico --add-data "beer.ico;." gui_app.py
```

### Инсталлятор
```bash
python build_installer.py
pyinstaller --onefile --name=SkufUp_Setup --icon=setup.ico SkufUp_Installer.py
```

---

## ⚙️ Настройка чувствительности

Отредактируй `config.py`:

```python
DETECTOR_SETTINGS = {
    # Порог похожести (0.55 = 55%)
    # Увеличь если много ложных срабатываний
    # Уменьши если не ловит банку
    "similarity_threshold": 0.55,
    
    # Порог громкости
    "peak_threshold": 0.1,
    
    # Пауза между срабатываниями
    "cooldown": 3.0,
    
    # Показывать уровень звука в консоли
    "debug_mode": True,
}
```

---

## 🐛 Отладка

Включи `debug_mode: True` в `config.py` и запусти через консоль:

```bash
python gui_app.py
```

Ты увидишь:
```
🔈 [████████████░░░░░░░░░░░░░░░░░] RMS:0.023 Peak:0.089
🔊 [████████████████████████████░] RMS:0.156 Peak:0.423
   📊 Похожесть на эталон: 67.3%
🍺 БАНКА ОТКРЫТА! (похожесть: 67.3%)
```

---

## 📝 Лицензия

Свободное использование. Делай что хочешь! 🍻

---

## 🤝 Авторы

Создано с помощью **GitHub Copilot** 🤖
